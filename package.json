{
  "name": "sqs-receiver",
  "description": "Fast and easy worker for processing large amounts of SQS messages with a high level of concurrency.",
  "version": "0.1.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "prepublishOnly": "npm run build",
    "lint": "eslint src/**/*.ts"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/thomazfeitoza/sqs-receiver.git"
  },
  "keywords": [
    "aws",
    "sqs",
    "worker",
    "consumer",
    "fast",
    "queue",
    "receiver",
    "message",
    "event-driven"
  ],
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1120.0",
    "bluebird": "^3.7.2"
  },
  "devDependencies": {
    "@types/bluebird": "^3.5.36",
    "@types/node": "^17.0.25",
    "typescript": "^4.6.3"
  }
}
